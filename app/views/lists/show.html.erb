<div class="navbar-lewagon d-flex align-items-center border border-dark">
  <h1 class="m-3"><%= @list.name %></h1>
  <%= link_to "Add a list", new_list_path, class: "btn btn-primary m-3"%>
</div>

<ul>
  <% @bookmarks.each do |bookmark| %>
    <li>
      <div class="card-movie d-flex flex-column m-3">
        <div class="d-flex flex-row align-items-center justify-content-between">
          <div>
            <h2><%=bookmark.movie.title%></h2>
            <p><%=bookmark.movie.overview%></p>
          </div>
          <div>
            <%= link_to "Delete",
              bookmark_path(bookmark),
              data: {turbo_method: :delete, turbo_confirm: "Are you sure?"},
              class: "btn btn-danger"
            %>
          </div>
        </div>
        <div class="img-movie">
          <img src=<%=bookmark.movie.poster_url%> alt=<%="Poster #{bookmark.movie.title}"%>>
        </div>
        <div>
          <p><%= bookmark.comment%></p>
          <p>Rating: <%= bookmark.movie.rating%></p>
        </div>
      </div>
</li>
  <% end %>
</ul>
<%= link_to "Add a movie", new_list_bookmark_path(@list), class: "btn btn-success"%>
<%= link_to "Back to list", lists_path, class: "btn btn-light"%>
